package:
  name: py3-azure-mgmt-trafficmanager
  version: 1.1.0
  epoch: 0
  description: This is the Microsoft Azure Traffic Manager Management Client Library.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py3-azure-common
      - py3-azure-mgmt-core
      - py3-msrest
      - python-3

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - wolfi-base
      - busybox
      - build-base

pipeline:
  - uses: git-checkout
    with:
      expected-commit: f2bfeae912535ff007d5b7b4e9a6cc9105cc233c
      repository: https://github.com/Azure/azure-sdk-for-python
      tag: azure-mgmt-trafficmanager_${{package.version}}
      destination: ./azure-sdk-for-python

  - runs: |
      mv ./azure-sdk-for-python/sdk/trafficmanager/azure-mgmt-trafficmanager/* .

  - name: Python Build
    uses: python/build-wheel

  - uses: strip

update:
  # The azure repo has some tags that have a bN suffix, which
  # is not a valid version, so ignore those.
  ignore-regex-patterns:
    - b
  enabled: true
  github:
    identifier: Azure/azure-sdk-for-python
    tag-filter-contains: azure-mgmt-trafficmanager_
    strip-prefix: azure-mgmt-trafficmanager_
    use-tag: true

test:
  environment:
    contents:
      packages:
        - wolfi-base
  pipeline:
    - runs: python -c 'import azure.mgmt.trafficmanager'
