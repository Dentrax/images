package:
  name: py3-azure-mgmt-servicebus
  version: 8.2.0
  epoch: 0
  description: This is the Microsoft Azure Service Bus Management Client Library.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py3-azure-common
      - py3-azure-mgmt-core
      - py3-msrest
      - python-3

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - wolfi-base
      - busybox
      - build-base

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 75897ac38f1882892962ce5b6c5fa045c9ade12f
      repository: https://github.com/Azure/azure-sdk-for-python
      tag: azure-mgmt-servicebus_${{package.version}}
      destination: ./azure-sdk-for-python

  - runs: |
      mv ./azure-sdk-for-python/sdk/servicebus/azure-mgmt-servicebus/* .

  - name: Python Build
    uses: python/build-wheel

  - uses: strip

update:
  # The azure repo has some tags that have a bN suffix, which
  # is not a valid version, so ignore those.
  ignore-regex-patterns:
    - b
  enabled: true
  github:
    identifier: Azure/azure-sdk-for-python
    tag-filter-contains: azure-mgmt-servicebus_
    strip-prefix: azure-mgmt-servicebus_
    use-tag: true

test:
  environment:
    contents:
      packages:
        - wolfi-base
  pipeline:
    - runs: python -c 'import azure.mgmt.servicebus'
