name: CI build action

on:
  pull_request:
    branches:
      - main
    paths:
      - "*.md"

jobs:
  build:
    name: Chainguard packages (CI)
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'