name: Openssl1

on:
  push:
    branches:
      - gh-readonly-queue/main/**
  pull_request:
    branches: ['main']
    paths:
      - "sub-os-openssl1/*.yaml"
      - "sub-os-openssl1/Makefile"
      - ".github/workflows/dag-push-enterprise-os-openssl1.yaml"
jobs:
  lint:
    name: Wolfictl Lint
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v3

      - name: Lint
        id: lint
        uses: docker://ghcr.io/wolfi-dev/wolfictl:latest@sha256:040ebe43f75a5ab8ec0b43b2ac522c8a966522b2205e6f63ee8f7744e5e0295f
        with:
          entrypoint: wolfictl
          args: lint sub-os-openssl1 --skip-rule forbidden-repository-used --skip-rule forbidden-keyring-used

      - name: Enforce YAML formatting
        id: lint-yaml
        uses: docker://ghcr.io/wolfi-dev/wolfictl:latest@sha256:040ebe43f75a5ab8ec0b43b2ac522c8a966522b2205e6f63ee8f7744e5e0295f
        with:
          entrypoint: wolfictl
          args: lint yam
