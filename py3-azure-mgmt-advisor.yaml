package:
  name: py3-azure-mgmt-advisor
  version: 9.0.0
  epoch: 0
  description: Microsoft Azure Advisor Client Library for Python
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py3-azure-mgmt-core
      - py3-msrest
      - python-3

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - wolfi-base
      - busybox
      - build-base

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 3b8d37430256cad9c968dc456c79cd5ec1720073
      repository: https://github.com/Azure/azure-sdk-for-python
      tag: azure-mgmt-advisor_${{package.version}}
      destination: ./azure-sdk-for-python

  - runs: |
      mv ./azure-sdk-for-python/sdk/advisor/azure-mgmt-advisor/* .

  - name: Python Build
    uses: python/build-wheel

  - uses: strip

update:
  # The azure repo has some tags that have a bN suffix, which
  # is not a valid version, so ignore those.
  ignore-regex-patterns:
    - b
  enabled: true
  github:
    identifier: Azure/azure-sdk-for-python
    tag-filter-contains: azure-mgmt-advisor_
    strip-prefix: azure-mgmt-advisor_
    use-tag: true

test:
  environment:
    contents:
      packages:
        - wolfi-base
  pipeline:
    - runs: python -c 'import azure.mgmt.advisor'
