package:
  name: py3-azure-mgmt-managementgroups
  version: 1.0.0
  epoch: 0
  description: This is the Microsoft Azure Management Groups Client Library.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py3-azure-common
      - py3-azure-mgmt-core
      - py3-msrest
      - python-3

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - wolfi-base
      - busybox
      - build-base

pipeline:
  - uses: git-checkout
    with:
      expected-commit: ea11f0a8bf1d54376f535610a875cfde66d2e5b2
      repository: https://github.com/Azure/azure-sdk-for-python
      tag: azure-mgmt-managementgroups_${{package.version}}
      destination: ./azure-sdk-for-python

  - runs: |
      mv ./azure-sdk-for-python/sdk/managementgroups/azure-mgmt-managementgroups/* .

  - name: Python Build
    uses: python/build-wheel

  - uses: strip

update:
  # The azure repo has some tags that have a bN suffix, which
  # is not a valid version, so ignore those.
  ignore-regex-patterns:
    - b
  enabled: true
  github:
    identifier: Azure/azure-sdk-for-python
    tag-filter-contains: azure-mgmt-managementgroups_
    strip-prefix: azure-mgmt-managementgroups_
    use-tag: true

test:
  environment:
    contents:
      packages:
        - wolfi-base
  pipeline:
    - runs: python -c 'import azure.mgmt.managementgroups'
