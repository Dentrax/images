package:
  name: py3-azure-mgmt-privatedns
  version: 1.1.0
  epoch: 0
  description: This is the Microsoft Azure DNS Private Zones Client Library.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py3-azure-common
      - py3-azure-mgmt-core
      - py3-msrest
      - python-3

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - wolfi-base
      - busybox
      - build-base

pipeline:
  - uses: git-checkout
    with:
      expected-commit: b26314d4a2f17d380a880c64dcf6fcb3d633a40b
      repository: https://github.com/Azure/azure-sdk-for-python
      tag: azure-mgmt-privatedns_${{package.version}}
      destination: ./azure-sdk-for-python

  - runs: |
      mv ./azure-sdk-for-python/sdk/network/azure-mgmt-privatedns/* .

  - name: Python Build
    uses: python/build-wheel

  - uses: strip

update:
  # The azure repo has some tags that have a bN suffix, which
  # is not a valid version, so ignore those.
  ignore-regex-patterns:
    - b
  enabled: true
  github:
    identifier: Azure/azure-sdk-for-python
    tag-filter-contains: azure-mgmt-privatedns_
    strip-prefix: azure-mgmt-privatedns_
    use-tag: true

test:
  environment:
    contents:
      packages:
        - wolfi-base
  pipeline:
    - runs: python -c 'import azure.mgmt.privatedns'
