package:
  name: azure-aad-pod-identity
  version: 1.8.15
  epoch: 1
  description: Assign Azure Active Directory Identities to Kubernetes applications.
  copyright:
    - license: MIT

environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - ca-certificates-bundle
      - busybox
      - bash
      - build-base
      - go

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Azure/aad-pod-identity
      tag: v${{package.version}}
      expected-commit: b64167ad192d8465ff3c6930a296a5d2169eece9

  - runs: |
      unset LDFLAGS
      make build ARCH=$(go env GOARCH)

  - runs: |
      install -Dm755 bin/aad-pod-identity/nmi "${{targets.destdir}}"/usr/bin/nmi

  - uses: strip

subpackages:
  - name: azure-aad-pod-identity-mic
    description: Azure Active Directory 
    pipeline:
      - runs: |
          install -Dm755 bin/aad-pod-identity/mic "${{targets.subpkgdir}}"/usr/bin/mic
      - uses: strip

update:
  enabled: true
  github:
    identifier: Azure/aad-pod-identity
    strip-prefix: v
    tag-filter: v
    use-tag: true
