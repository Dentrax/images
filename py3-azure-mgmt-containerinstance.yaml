package:
  name: py3-azure-mgmt-containerinstance
  version: 10.1.0
  epoch: 0
  description: This is the Microsoft Azure Container Instance Client Library.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py3-azure-common
      - py3-azure-mgmt-core
      - py3-msrest
      - python-3

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - wolfi-base
      - busybox
      - build-base

pipeline:
  - uses: git-checkout
    with:
      expected-commit: bd5626676a50e43cc3f53d8979c23b3df1219449
      repository: https://github.com/Azure/azure-sdk-for-python
      tag: azure-mgmt-containerinstance_${{package.version}}
      destination: ./azure-sdk-for-python

  - runs: |
      mv ./azure-sdk-for-python/sdk/containerinstance/azure-mgmt-containerinstance/* .

  - name: Python Build
    uses: python/build-wheel

  - uses: strip

update:
  # The azure repo has some tags that have a bN suffix, which
  # is not a valid version, so ignore those.
  ignore-regex-patterns:
    - b
  enabled: true
  github:
    identifier: Azure/azure-sdk-for-python
    tag-filter-contains: azure-mgmt-containerinstance_
    strip-prefix: azure-mgmt-containerinstance_
    use-tag: true

test:
  environment:
    contents:
      packages:
        - wolfi-base
  pipeline:
    - runs: python -c 'import azure.mgmt.containerinstance'
