#nolint:bad-version
package:
  name: py3-azure-mgmt-iotcentral
  # Note that this is beta (I think by the naming convention at least).
  version: 10.0.0_b2
  epoch: 0
  description: This is the Microsoft Azure Iot Central Management Client Library.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py3-azure-common
      - py3-azure-mgmt-core
      - py3-msrest
      - python-3

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - wolfi-base
      - busybox
      - build-base

# transform melange version to remove "_" so we can use a var in the tag.
var-transforms:
  - from: ${{package.version}}
    match: \_b
    replace: 'b'
    to: mangled-package-version

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 002d008dfa92540602ae4bbeecff7c37e3792ba3
      repository: https://github.com/Azure/azure-sdk-for-python
      tag: azure-mgmt-iotcentral_${{vars.mangled-package-version}}
      destination: ./azure-sdk-for-python

  - runs: |
      mv ./azure-sdk-for-python/sdk/iothub/azure-mgmt-iotcentral/* .

  - name: Python Build
    uses: python/build-wheel

  - uses: strip

update:
  enabled: true
  github:
    identifier: Azure/azure-sdk-for-python
    tag-filter-contains: azure-mgmt-iotcentral_
    strip-prefix: azure-mgmt-iotcentral_
    use-tag: true

test:
  environment:
    contents:
      packages:
        - wolfi-base
  pipeline:
    - runs: python -c 'import azure.mgmt.iotcentral'
